# Handoff

Rewritten for a fresh session at HEAD `28883af`, kept current through the MCP tool bridge, its live acceptance, and the whole-tree adversarial review that followed. It describes the tree as it is now, not the audit narrative it grew out of — that history lives in git and in `docs/verification/README.md`.

This is the only session handoff file. Update it in place when the active task changes; do not create dated handoff/plan/session-summary files.

## Immediate task

The Codex-then-Antigravity bugfix queue in `docs/verification/rc3-review.md` addendum B is done on this tree (unit tests). Do not add a Codex rate-limit recognizer without a captured live App Server error string. Empty `subagent()` inherit is stock DSH and stays that way. Live Codex/Antigravity suites have not been re-run on this cut.

## Current branch/state

```text
feat/core-provider-plugins-rc3
```

Six packages at `0.1.0-rc.3`, unpublished. **Do not trust a commit count or a file list in this paragraph** — two sessions have been committing here on the same day and every previous count in this line went stale within hours. Read `git status` for what is uncommitted and `git log origin/feat/core-provider-plugins-rc3..HEAD` for what is unpushed; `origin` was last at `3064cdb`. What is durable: everything since that commit is unpushed, and **a second session is working in `packages/codex`** on the §7d stepped-turn transport, which landed in five parts during 2026-09-03 and whose live acceptance was still in progress. The documentation pass recorded below is docs-only and touches no package source, so it does not conflict with that work. Pushing this branch is all that has ever happened here: nothing is merged, tagged, released or published.

Only supported DSH generation:

```text
dsh-v0.1.2-alpha.1
cd5ef8148158c3a752a658978873241fdf8e2bbc
```

`0.1.1-rc.2` and earlier are **not supported**. Every declared peer, the dev graph and the whole test suite say exactly that. `docs/README.md` owns the policy and the *Local setup* note explaining why `pnpm install` needs the local upstream checkout.

Windows: **NOT TESTED**. No publish, merge, tag or release is authorized.

## Read before editing

1. `docs/README.md`
2. this file
3. `docs/ROADMAP.md`
4. `docs/ARCHITECTURE.md`
5. target package README
6. target package source/tests
7. `docs/verification/README.md` when exact accepted evidence matters
8. `docs/prior-art.md` and `docs/verification/*-cli-contract.md` when the task touches a vendor's surface or repeats a question someone outside this tree has already answered

`docs/verification/gemini/LATEST.md` is the rolling raw report, not the durable ledger.

## Where the packages stand

All four thawed packages are **THAWED, PENDING INDEPENDENT VALIDATION**. Nothing here is frozen, and the only thing standing between this tree and a freeze claim is a validator who did not write the code.

| Package | State |
|---|---|
| Core | remediated; Model Accounts surface removed outright; sidebar provider selection and ordering added; invalidation listeners contained the way change observers already were; 202 tests |
| Project Memory | recovery read race fixed, and a second one in the writer-lock release window; 79 tests |
| Codex | **transport replaced 2026-09-03**: one completed vendor turn per DSH step under `turn/start.outputSchema`, replacing the parked `item/tool/call` design (`ROADMAP.md` §7d). The checkpoint now carries a durable digest of the history it was taken against, a tool-call turn checkpoints, auxiliary requests are isolated on their own ephemeral thread, and the parked machinery is deleted along with `tools.ts`. Earlier work stands: provider audit, thread resume/rollback/fork, `thread/inject_items` verified, version floor rather than pin. The context window is now read from the vendor instead of being absent -- it is published on `thread/tokenUsage/updated` and nowhere else, already discounted by the vendor's 5% reserve, so DSH's pressure compaction has a real denominator and a user's own `~/.codex/config.toml` override is honoured for free. 151 tests; all six live suites re-run green on this tree, plus a product-level run in the real `web` profile |
| Antigravity | one live `agy` child per DSH session with `full`/`delta` envelopes; the vendor's published contract read end to end and every dependency inventoried in `docs/verification/agy-cli-contract.md`; the forced-schema transport stamps every turn and refuses a decision belonging to an earlier one, after `structured_output` was found to go stale; grants read from both stores the vendor honours; five defects two independent reviewers found, fixed and each pinned by a test that fails when its fix is reverted; **the `mcp-bridge` transport removed outright** and the `transport` config key with it, on contract exposure, irreducible setup and a blocking hole both reviewers found independently; the per-session in-flight guard and `source` in the message digest, both from the same review; quota narrowed to this package's own `agy` child, with the machine-wide process scan and its CSRF-token lifting removed, and then **turn-free quota** on top of it -- a child spawned for the reading alone, so the pre-turn blind window is gone (verified cold-equals-warm on one account, `usedPercent` 1.03 both ways); **a turn settles with a typed reason** rather than `status === 'SUCCESS'`, so a cancellation reports `ABORTED` and an unsettled turn a protocol violation; **a failed turn names the vendor build** it ran against, read once per adapter and awaited nowhere; 143 tests, and all seven live suites PASS on real `agy 1.1.24`/`1.1.25` including `test:live:agent-loop` (4/4) and `test:live:quota` (2/2, the second turnless) |
| Claude | usage-only stub, unchanged; 6 tests |
| Suite | Orchestrator preset now allows cross-route subagents; a failed preset update no longer strands its staging directory; 17 tests |

## Evidence on this tree

- `pnpm verify:local` exits `0` on three consecutive runs;
- **Codex live, re-run in full on 2026-09-03 against the stepped transport** — every suite, because the transport changed and a suite not re-run after that is not evidence for the tree that changed: `test:live:primary` 1/1, `test:live:acceptance` 9/9 covering all 15 scenarios (real DSH tool execution with turn continuation, cancellation during that continuation, stale-checkpoint and deleted-thread recovery, adversarial host-capability isolation, zero process residue), `test:live:inject-items` 1/1, `test:live:tool-result-continuation` 1/1, `test:live:web-search` 1/1 and `test:live:web-search-routed` 1/1. The two search suites still need `DSH_LIVE_CODEX_SEARCH_MODEL` — `gpt-5.6-sol` was used — and fail a **precondition assertion** without it: a harness prerequisite, not a product defect. **The first `primary` run failed, and that is the finding worth keeping**: a request with no tools sends no `outputSchema`, so the model answered in prose and the decision parse — gated on "is this auxiliary" rather than "did we send a schema" — failed the turn with `response is not valid JSON`. 560 unit tests had not caught it because every one of them supplied tools. Fixed, covered, and the suite re-run green;
- **product-level acceptance in the real `web` profile, 2026-09-03** — one session covering three of §6's open rows: Codex primary with Project Memory and routed search, the Codex -> Antigravity switch mid-session, and a memory written before the switch read back after it. Evidence is the durable session log rather than the model's account, with a planted marker (`ACCEPT_MEM_...`) so the memory claim could not be met by a plausible sentence, and the routed search attributed by process (`codex exec --ephemeral --cd /tmp/dsh-web-search-codex-*` running, no `agy` anywhere) rather than by the shape of its output. It also showed the DSH-minted `codex-<uuid>` call ids and `contextWindow: 258400` reaching the product. Usage & Limits stays open: Codex reports live, Antigravity reports unavailable. Full write-up in `docs/verification/README.md`;
- Antigravity live, re-run **after** typed turn settlement, the ungated `agy --version` read and turn-free quota, on real **`agy 1.1.25`** (the vendor self-updated from `1.1.24` mid-pass): `test:live:primary` 8/8, `test:live:session-continuation` 1/1 and `test:live:quota` 2/2, each read from its own exit code. The continuation suite asserts what a surviving turn cannot — that one child served both DSH steps, and that the model's answer carries a value it could only have read from the tool result — and it was also the only thing that caught the `--version` spawn inflating its own turn-spawn count, because live suites sit outside `pnpm test`. The quota suite's second scenario runs with no adapter, no turn and no session, and agrees with the turn-harvested figure to the second decimal;
- cross-route delegation, exercised end to end in the real `web` profile for the first time: parent Codex -> child Antigravity, parent Antigravity -> child Codex, and one parent turn with two concurrent background Codex children. All pass, evidenced by each child session's own `request/header` route in the durable log rather than by the model's report. Up to six concurrent `codex app-server` processes were observed, with no residue afterwards. The same run found the mid-turn route-switch defect, which is now fixed (`ROADMAP.md` §2) and covered by `test:live:tool-result-continuation` — a live probe that asserts the model actually saw the tool result, not merely that the turn survived;
- **the three items below name `test:live:mcp-bridge` and the MCP bridge's vendor probes. That transport was removed on 2026-09-03 and the suite deleted with it; the entries stay as the record of what was established, not as coverage of anything shipping.**
- four live suites added on 2026-08-31 and passing: `test:live:mcp-bridge` (the model calls a DSH tool natively, DSH executes it, and the result reaches the model inside the same vendor turn, in one child and one turn), `test:live:agent-allowlist` (a `finish`-only agent reaches none of the vendor's own file tools, and its turn still succeeds), `test:live:inject-items` (a value reachable only by injection comes back in the answer, with the JSON-RPC sniffed to prove the split) and the existing `test:live:tool-result-continuation`, re-run after the duplication was removed;
- a vendor-behaviour probe settled how `agy 1.1.22` treats the environment of the MCP servers it launches, at no quota cost — servers start before any stdin, so no turn runs. It passes its environment **verbatim** (95 keys in, 95 keys out, nothing injected and nothing dropped), `agy mcp add --env` **merges** with that rather than replacing it, and the MCP child's `ppid` is still the `agy` pid directly. The first fact is what the socket-and-token addressing rests on; the second bounds it, since the environment reaches every server the vendor launches, third-party ones included;
- `test:live:mcp-bridge` re-run after that redesign, against real `agy 1.1.22`: the real vendor carried the token to the real bridge server and the tool result reached the model inside the same vendor turn;
- **`test:live:primary` 8/8 and `test:live:session-continuation` 1/1, re-run on this tree** — and the first attempt failed **two** suites, which is the finding worth keeping. Neither failure was a product defect: both fixtures had gone stale when `mcp-bridge` became the default on 2026-08-31 and neither suite had been run since. `session-continuation` counted the adapter's new `agy mcp list` precondition as a turn child, so "one child served both steps" read 2; and the `DSH Tool Loop` scenario passed no `sessionId`, which the default transport requires because its vendor turn spans DSH steps — so that scenario had been asserting a tool loop the shipped default cannot run at all. Both fixtures fixed, both suites green afterwards. **The lesson is about evidence, not about the fixtures**: "primary 8/8" was recorded before the default flip and carried forward as if it still described the tree. A live suite that is not re-run after a default changes is not evidence for the tree that changed;
- an adversarial code review and a documentation audit were run over the whole change set. Both found real defects; all are fixed.
- a whole-tree adversarial review by two models followed (`docs/verification/gemini/LATEST.md`): 16 findings on a green tree, all closed — 15 fixed, one rejected on the upstream subprocess contract, one referred to the maintainer and decided. The two models disagreed usefully; one called project memory and core clean while the other found a real race in each.

A green gate plus live suites is **not** an acceptance. See *What remains*.

## What was done

Ordered by how much it changes the contract.

- **DSH baseline moved to alpha.1 everywhere** (`2ae63bc`). Every peer range, the dev graph, `DSH_COMPATIBILITY_VERSION` and the contract-verifying scripts. Removed with it: the rc2/alpha `Function.length` arity probe and Core's two retired rc.2 dev fixtures. Two real incompatibilities surfaced and were fixed — `CallId` renamed to `ToolCallId` in `dsh-llm`, and `Context.slots` moving from `dsh-client-ui-slots` to `dsh-client-ui-renderer/client`.
- **Model Accounts removed from Core** (`a8b44fa`), together with the provider-declared `account` capability and its declarations in Codex and Claude. Removed rather than disabled. No Core path reads or mutates a vendor credential record any more.
- **Codex thread handling redesigned** (`79fa972`). An ordinary turn resumes instead of forking; rollback realigns on divergence; fork is kept only for a checkpoint that is neither the tip nor an ancestor. Motivated by measurement, not taste — see *Codex vendor threads* in `ARCHITECTURE.md`.
- **Project Memory recovery read race fixed** (`e38ce06`). A benign concurrent journal rewrite failed an unrelated caller's memory operation. Found only because `pnpm verify:local` was run repeatedly; a single pass stayed green throughout.
- **Vendor-authored text no longer reaches the model or the user** anywhere in the suite. Antigravity gained `antigravityVendorFailure`; Codex's remaining two sites were closed, one of them only after a review caught that a commit had already declared the work finished.
- **Antigravity provider stage**: audit, 7 → 73 tests, catalog parsing rewritten against the format the vendor really emits, vendor sandbox flag added, intra-package duplication removed, usage harvested from the provider's own turn process.
- **Antigravity reasoning efforts separated from model ids** (`5d74408`, with a follow-up). The vendor ships `gemini-3.7-flash-low/-medium/-high` as three models; the adapter now advertises one model with three efforts and maps the invocation back, keeping the suffixed ids as aliases. The follow-up made that mapping resolve through discovery instead of peeking at the model cache, which could otherwise send a suffixed id and a contradicting `--effort` once the cache had expired, and kept a catalog outage from losing an otherwise valid turn. A base id without an explicit effort defaults to `high` by deliberate choice. See *Antigravity model catalog and reasoning efforts* in `ARCHITECTURE.md`.
- **Codex no longer fails a turn over a context block it cannot carry** (issue #4). A stopped subagent's settlement notice quotes the interrupted child's terminal output, `tool-call` blocks included, into a `user` message. The plugin rejected those blocks, which killed the live turn and — with no checkpoint written — every later replay of that session. They are now projected to text on the transient request; durable history is untouched. Covered by focused tests only: it landed after the Codex live acceptance run above.
- **A subagent may now run on another primary route.** The Orchestrator preset mounts `subagent` with `modelSelectionSettings: true`, which is all the Suite owns here: a spawned child is an ordinary DSH agent reaching its model through `ctx.llm`, so every registered route — `codex-app-server` and `antigravity-cli` included — was already usable as a subagent model and nothing provider-specific was needed to make it so. The host settings singleton stays the surrounding profile's (the web-app bundle mounts it), and the allowlist stays a user authorization that is off by default. `subagent_fork` keeps selection off so the inherited prefix stays KV-Cache-eligible. Suite 12 -> 16 tests. See *Subagent model routes* in `ARCHITECTURE.md` and `ROADMAP.md` §7c.
- **Antigravity now holds one live `agy` child per DSH session instead of spawning one per step.** Every step used to be a whole process — primary, subagent and routed search alike — so every step opened a fresh vendor conversation, guaranteed a prefix-cache miss, paid a cold CLI start in the critical path, and handed the model its own past actions as JSON quoted back at it rather than as its own turns. `agy --input-format stream-json` already ran one turn per NDJSON line, and `--json-schema` turned out to be enforced per turn rather than only on the last, which was probed against the real binary before any code was written. Measured on `agy 1.1.22`, a continuation inside one child read 20418 of a 23496-token prefix from cache and paid ~3.3k new tokens instead of ~23.5k. Reuse is gated on the request extending exactly the message ids already delivered; anything else rebuilds. Two defects were found and fixed while doing it: the vendor's cumulative per-conversation usage would have been summed again by the token meter, and model-authored tool-call ids repeat across steps. See *Antigravity session-lived vendor conversation* in `ARCHITECTURE.md`.
- **Antigravity's forced output schema now types tool arguments.** It declared `arguments: {"type":"object"}` for every call, which an empty object satisfies — so a call with none of the tool's required fields was well-formed to the vendor, failed in DSH, and was retried by a model with no way to see why. Each variant now pins one tool name and that tool's own parameter schema. The shape (`anyOf` plus an `enum`-of-one discriminator) was probed against the real binary before it was written, and DSH schemas are rewritten into the vendor's subset first, with an unrepresentable schema abandoning that one tool rather than the whole catalog.
- **Three defects found by reading real session logs.** Worth naming because none of them failed a test, and two were silent by construction. `maxTokens` was refused on every request, so the new context capacity made compaction run and fail 35 times in one session while `agent/pre-step` logged a warning and carried on; it is now accepted and ignored for auxiliary calls only. The divergence check compared message ids, but the tool-result pruner rewrites a message's content and keeps its id, so 80k tokens of pruning never reached the vendor; it now digests content. And a delta echoed the conversation's own replies back at it, which doubled the density of every action in the transcript and helped a real session end in 43 identical `todo_write` calls after the work was done.
- **Antigravity advertises a context capacity.** Without one, `compaction-basic` refuses automatic pressure compaction and the refusal is swallowed as a single warning, so the route never compacted and had no bound on history growth at all. Codex still discloses none either; it is left alone deliberately because guessing a per-model window is not an adapter's decision (`ROADMAP.md` §3).
- **The Usage sidebar can be told which providers to show, and in what order.** Authored by Gemini 3.7 Flash driving this workspace through DSH, then reviewed and verified before landing. Resolution stays dynamic — ordered ids that are still registered first, newly registered ones appended and visible, saved ids no provider claims ignored — and no provider id is hardcoded. With no saved setting the rendering is exactly what it was. The preference is per browser, through a storage seam with a `localStorage` default; that is a deliberate choice for a display preference and not a DSH setting, worth revisiting if it ever needs to follow a user across machines. One defect was found in review and fixed on top: reordering while a provider was unregistered used to discard that provider's remembered slot. Core 182 -> 199 tests.
- **Web search left as it is, deliberately.** The investigation found the suite already routes search through the session's live primary model — the concern that prompted it was unfounded. What did change: search results now carry an untrusted-content notice (`81ca500`), which they previously did not.

- **The Antigravity MCP tool bridge is adopted, and half of it is built** (`a42dcc4`). Antigravity was the outlier: Codex stopped being a dumb model endpoint at its thread redesign, declaring the DSH catalog to the vendor and parking the vendor's tool request while DSH's own loop executes it, while Antigravity still strips `agy` to `tools: [finish]` and forces its reply through `--json-schema` — the last remaining generator of the repeated-tool-call pathology, since combining tools with a strict response schema is a documented cause. `agy` has no App Server equivalent, so MCP is the same design through the only door it offers. Four probes against real `agy 1.1.22` settled the shape: a call blocked 9.2s keeps the turn open and its result reaches the model inside it; a stdin line written during a blocked turn is buffered rather than interleaved, so the bridge and the session-lived child do not conflict; the vendor prefix still caches with an MCP catalog present (~57.2k of a 62k prefix read from cache per step, ~5.4–5.9k new); and there is exactly one server process per `agy` process whose `ppid` is the pid the adapter spawned. That last fact was both the correlation key and the isolation boundary — **superseded**, see *The bridge is addressed directly* below; the key is now a token and the boundary is its single-use binding. A server no live adapter claims is still served an empty catalog, which is what bounds the global-registration exposure the maintainer accepted. Landed: the host, the server process, their protocol, and a `transport` flag defaulting to the shipped `schema` path, so rollback is one config key rather than a revert. The adapter wiring landed on top: the transport now runs end to end, racing an open vendor turn against a bridge call, answering a blocked call from DSH's history without writing a new vendor turn, and refusing to start at all when `agy mcp list` does not show the bridge server -- an unregistered bridge would otherwise hand the model no tools and read as a disobedient model. **Live acceptance passes** against real `agy 1.1.22` (`pnpm test:live:mcp-bridge`): the model calls a DSH tool natively, DSH's loop executes it, the result reaches the model inside the same vendor turn, and one child and one turn serve the whole loop. `--sandbox` turned out fine and stays. Two assumptions were wrong and cost the first two live runs: naming `call_mcp_tool` in the agent allowlist terminates the agent, and MCP tools are not gated by that allowlist at all -- so the bridge's agent now carries exactly the schema transport's `finish`-only list. One defect fixed: the `BLOCKED_NATIVE_TOOLS` backstop rejected the finished turn for using `call_mcp_tool`, now exempted and covered deterministically. **`mcp-bridge` is now the default** (maintainer decision, 2026-08-31). That is a breaking change for deployment, not for code: a fresh install does nothing until `agy mcp add` has registered the bridge server and `mcp(dshtools/*)` is granted, and the first turn says so with the resolved path. No silent fallback, by design. Antigravity 99 -> 118 tests. A second live suite settled the isolation question the bridge work raised: `test:live:agent-allowlist` observes that a `finish`-only agent invoked no native tool, leaked nothing from a file inside its own granted workspace, and still completed its turn -- so the allowlist prevents, and `init.tools` listing all 57 tools is the CLI's registry rather than the agent's effective set. That half of the isolation claim had never been observed before, on either transport. See *Antigravity MCP tool bridge* in `ARCHITECTURE.md` and `ROADMAP.md` §3.

- **The bridge is addressed directly, and the transport's own state stopped duplicating itself.** Four changes on top of the bridge, the first a defect. A vendor turn here spans DSH steps because it blocks inside the MCP call, and everything that rewrites history — rewind, compaction, repair — lands in exactly that gap; the resume path answered the blocked call without ever testing that DSH's history still agreed with what the conversation had been told, which the schema path has tested on every step since it was written. Silent by construction: the model reads a plausible result and keeps going against a conversation rewritten underneath it. Then `bridgePending` went, because the bridge channel already held the outstanding call and only a separately-minted DSH id kept them apart — the server now mints `agy-mcp-<uuid>` and it crosses into DSH verbatim (a UUID, not a counter: a changed signature rebuilds the child mid-session and a counter would collide with ids already in durable history). `inFlight` and `turnAbort` became one `openMcpTurn`, since a turn's promise and its cancellation have identical lifetimes here. Last and largest, the claim protocol: the whole scan-and-claim apparatus existed only because the adapter learned its child's pid *after* spawning, so a hello could arrive before its channel was registered. A token minted *before* the spawn and passed through the environment removes the ordering problem outright, and with it `earlyHellos`, the claim window, both hold timers, `listAdapterSockets`, `findAdapter`, `firstClaim` and the multi-socket offer fan-out. The token is readable by co-resident MCP servers — the probe proved that, not argued it — so it binds exactly once: a second claimant is refused and an impostor winning the race gets the real server refused and the turn failing loudly, never a third party quietly served DSH's tools. Live-verified. The schema transport also moved to `schema-transport.ts`, a pure move checked byte-for-byte, leaving `antigravity-primary.ts` at 1794 lines from 2125. Steps two through five were implemented by `agy` (Gemini 3.7 Flash High) under review; what review caught was never logic but rationale — one deleted invariant left uncovered, and a comment justifying the private-directory check by the very fact the change had just made false.

- **The same continuation defect was found in Codex, by analogy rather than by a test.** Antigravity's suspended-turn fix above described a shape, not a quirk, so the App Server path was read for it: a turn parks inside `item/tool/call` and spans DSH steps, `startTurn` is skipped on a continuing step, and with it the checkpoint reconciliation that realigns the thread. The continuation checked model and tool catalog; a rewrite of the history *before* the parked call was checked nowhere, so the model resumed from a prefix DSH no longer had. The turn now ends and the next request realigns through `startTurn`. Narrower than the Antigravity case — a deleted result and a trailing assistant message already failed loudly, and the next turn's checkpoint comparison self-corrects — so it damages one turn's reasoning rather than a session, and is recorded that way. Codex 90 -> 92 tests. That third data point is what let the standing Core question finally be decided (`ROADMAP.md` §3, closed 2026-09-01): **the shape stays in the providers, the invariant moves to §5**. The mechanisms diverge irreducibly — a child fed NDJSON envelopes versus a server-side checkpointed thread, rebuild versus rollback/fork — so an abstraction over both would be parameterized on every axis that differs. And neither occurrence of the defect was caused by missing shared code; a helper is as easy to not call as a check is to not write. The message-digest function stays duplicated on purpose: five identical lines at two call sites is not an abstraction, and what matters is that the two do not drift apart in meaning, which the invariant pins. Revisit when a `claude -p` primary route makes the third instance real.

- **The external survey and the two vendor contract reads are written down** (this pass, docs only, no code touched). Three new or changed documents, registered in `docs/README.md` under a new *Supporting documents* section, which also gave the previously unregistered `agy` contract document a home. `docs/prior-art.md`: the three ways other tools drive a vendor CLI — TUI screen-scrape, structured protocol, client impersonation — with `awslabs/cli-agent-orchestrator` read closely enough to record what tier 1 costs, the Claudexor lane model set against this suite's step model, the Agent Client Protocol read as a vocabulary rather than a dependency, and the two places this tree is measurably ahead (`vibe-kanban` drives Codex through the same App Server and forks every follow-up, which fork/resume measurement already priced at 0 against ~3840 cache credit; its exact version pin against this tree's floor). `docs/verification/claude-code-cli-contract.md`: a pre-implementation read of `claude 2.1.246` for a primary route, its twenty prospective dependencies with status, three traps and five ordered probes. `agy-cli-contract.md` gained findings 7-11 and one correction: the quota surface enumerated rather than inferred (no `quota`/`usage`/`credits` subcommand exists on 1.1.24), the `businessaicode.v1beta`/`v1main` `FetchQuotaStatus` siblings that make finding 5's "no fallback" line too strong, the vendor's own `Trajectory has exceeded max length` trimming as a divergence direction `sentDigests` cannot see, client-side slash interception in print mode, and what `agy` is (a Go binary named `jetski` built on the Codeium/Windsurf language-server stack, which is why quota lives on a loopback server). Four items became `ROADMAP.md` work: typed turn settlement over `agy`'s seven statuses and cancellation as a value, declared per-route capabilities, quota without a prior turn, and vendor-side trajectory trimming. §4 was rewritten: the Claude Code terms question is **settled against published text** at `code.claude.com/docs/en/legal-and-compliance`, which replaces the 2026-08-31 support-chat basis, and it carries two obligations that are not code — the distributor accepting Commercial ToS, and dropping the mark from the npm name and the glyph before publication.

## What remains

1. **Independent validation, then freeze.** This is the only blocker for all four packages. Everything below is optional next to it. The reason to insist keeps getting stronger: **three** reviews that were deliberately not told the author's reasoning have now each found defects the author's own green test runs had missed. A whole-tree pass by two models over four areas (`docs/verification/gemini/LATEST.md`) then reported **16** findings on a tree whose own gate was green: 10 confirmed so far, including a security defect and a blocking one in code written the same day. The two models disagreed usefully -- one called project memory and core clean while the other found a real race in each.

**Every finding has now been verified and dispositioned**: 15 confirmed and fixed, one rejected (`terminate()` does escalate -- the upstream contract says the grace period is for exactly that), and one referred to the maintainer rather than quietly flipped -- `latestCheckpoint` failing closed on a prior response with no usable checkpoint -- **decided in favour of rebuilding**: the scan now continues backwards to an older usable checkpoint, keeping the vendor's prompt cache, and rebuilds the conversation only when there is none. Both touched providers were re-run live afterwards.

Note what none of these reviews is: their charters, area briefs and the reading of every finding were produced by the party that wrote the code, and nobody outside this workspace has signed anything.
2. ~~**`thread/inject_items` is unverified.**~~ **Closed 2026-08-31**: it reaches the model, verified by `pnpm test:live:inject-items`. The probe puts a value where injection is the only path to it -- a tool result is not user-authored, so it lands in the injected half while a trailing user question is the whole turn input -- and asserts that split by sniffing the JSON-RPC before reading the answer, which was exactly the injected value.
3. **alpha.1 runtime probe as separate evidence.** It has effectively dissolved into the ordinary test run now that the whole workspace builds and tests against alpha.1, but it has not been repeated as a distinct artifact.
4. **Suite preset bridge may be obsolete.** It exists to work around an rc.2 launcher bug that overwrites third-party preset roots. Nobody has checked whether alpha.1 still has that bug. If it does not, remove the bridge rather than carry it.
5. ~~**The tool-result continuation duplication.**~~ **Removed.** It existed only because item 2 was unverified. The results now reach the model once, through the imported history, and `test:live:tool-result-continuation` was re-run against the real vendor afterwards: the model still reported the marker, so the single carrier works.
6. **Accepted debts**, recorded with reasoning and revisit conditions in `ROADMAP.md` §3. The Antigravity vendor-version debt is **closed as of 2026-09-02**, and not by paying it: a version range was examined and rejected as the wrong instrument — `agy` is user-installed and self-updating, so an upper bound refuses every good new release while still believing a bad patch inside the range. Two of the three behaviours it stood in for already failed loudly (`attached() === false`), and the third, the silent one, is now asserted directly: `openVendorTurn` makes a turn's settlement synchronously readable and the resume path refuses to answer a parked call into a turn that has already finished. Demonstrated before it was fixed; Antigravity 131 -> 133 tests. What remains in §3 is the `usage-source.ts` loopback TLS trust model, plus a deliberately smaller open item: record `agy --version` in diagnostics without gating on it.
7. **Blocked on upstream**: publishing `0.1.2-alpha.1` to npm. Until then the declared ranges are uninstallable from the registry, which gates publication rather than development, and the local-checkout overrides stay.

8. **A vendor turn that ends around its own parked tool call — guarded in both providers, root cause found, and the real fix DECIDED: a stepped-turn transport for Codex, to be implemented next session (§7d).** Started from a real bug report: the model asked a question with `ask_user_question`, the turn ended before the answer, and the answer was not used. Traced in the maintainer's session log to the vendor emitting the question AND the final answer while DSH spent 476 s on a preceding human-approved `edit`. The root cause is Codex Code Mode: a long nested tool becomes a yielded cell, the model is re-invoked at every yield boundary, and on one of those it decides to stop waiting — which is also why it is intermittent and worsens with context size, rather than firing at a fixed timeout. Both providers now refuse to answer into an abandoned call instead of recording the model's invented reply. The vendor-side cure is measured and available — `features.code_mode.direct_only_tool_namespaces` took billed turns during a 600 s wait from many to zero, and `tool_timeout_sec` per server raises the call's ceiling, both settable per-thread without touching user configuration — but that cure is an undocumented config key, and independent verification then showed the path this suite already ships is undocumented too: **`dynamicTools` on `thread/start` is not in the published contract**, while `outputSchema` is. **So the maintainer decided on 2026-09-02 to move Codex to a stepped-turn transport — one completed vendor turn per DSH step, the model answering under `turn/start.outputSchema` — and to implement it in a later session.** That removes this defect by construction rather than by guard, and makes the MCP bridge obsolete rather than deferred. The implementation brief, the measured working schema shape and its five strict-mode rules, and the pitfalls that must not be rediscovered (optional parameters become nullable unions and the adapter must then strip nulls; DSH must mint call ids; results go through `thread/inject_items`) are all in `ROADMAP.md` §7d. The evidence, including two of the author's own conclusions that independent verification refuted, is in §5. Keep the guard until the parked path is actually gone.

Decided and closed, so nobody reopens them: old vendor threads already in the user's Codex account are not being cleaned up; web search keeps routing through the session's primary model.

Two more, decided 2026-09-01 while simplifying the Antigravity bridge, recorded because both are the kind of thing a later reader would otherwise redo:

- **the `schema` transport's tool path is NOT being deleted.** The obvious cleanup once `mcp-bridge` became the default, and deliberately declined. `mcp-bridge` needs a once-per-machine `agy mcp add` plus a grant in user-owned vendor configuration, and it hard-fails by design when either is missing; `transport: "schema"` is the only remedy that lives inside this repository, and it still carries the larger body of live evidence. Revisit once the bridge has accumulated comparable live evidence — not before, and never as tidying;
- **`runTurn` and `runMcpTurn` are NOT being merged behind a shared `prepareSession`.** They look near-identical and are not: one handles a request with no session id by opening a one-shot child, the other refuses such a request outright; one commits `sentDigests` only on success through a local, the other inside `settleMcpStep`; and they select different workspaces and vendor agents. The shared part is real but thinner than it looks, and this was the change most likely to introduce a silent regression for the least gain. Reconsider only if the two paths converge on their own.

## Working notes that cost real time to learn

- `DSH.md` reaches every request on every route: `nishi-dsh-project-memory` reads it and renders it as `## Project Contract (DSH.md)`, and the runtime keeps that block visible across compaction. It is therefore the cheapest place to put behaviour that must always be in front of the model, and it needs no preset change. Its *Working Discipline* section was added after two real sessions failed in ways nothing in the tree corrected: one repeated `todo_write` 43 times after the work was finished, another wrote a task list once and never touched it again across 157 steps.
- `@deepseek-ai/dsh-agent-instructions` is mounted in the Orchestrator preset with `instructionFileCandidates: []` and `localInstructionFileCandidates: []`, so `AGENTS.md` / `CLAUDE.md` discovery is switched off. Upstream defaults are `['AGENTS.md', 'CLAUDE.md']`. Deliberate or not, it means the mechanism every vendor guide recommends for steering an agent is unavailable here, and `DSH.md` is the only always-on instruction surface. Note also that `~/.dsh/AGENTS.md` would be read by BOTH plugins if it existed, and project-memory reads it into its byte budget without ever rendering it.

- **A fresh `codex app-server` process is ~3,535 input tokens cheaper for its first ~30 seconds.** Something host-side finishes registering in that window and lands in the prompt (`mcpServerStatus/list` visibly grows over it). Any A/B against this App Server MUST counterbalance arm order or spawn a fresh process per arm and start each thread at the same elapsed time — otherwise whichever arm runs first wins by 3.5 k per request. This cost a whole evening's conclusions once. The production path is immune, because `isolationConfig()` removes the surface that grows: with it, cold and warm both measure identically.
- **`tokenUsage.last.totalTokens` is not a cost and must never be summed across requests.** It is the breakdown for the most recent model request and its `inputTokens` is that request's ENTIRE context. Summing N of them counts the base prompt N times. Compare per-request uncached input plus output, or read the `total` breakdown the notification also carries.
- **A raw App Server probe is not the product.** Without the overrides `isolationConfig()` sends, first-turn input on a one-sentence task is ~14.4 k; with them it is 5,423, and with empty `baseInstructions` 2,013. Roughly 70% of a naive probe's input is host surface the provider switches off, so absolute numbers from such a probe describe the probe.
- Codex has no published App Server protocol documentation, but the binary generates its own: `codex app-server generate-json-schema --out DIR` and `codex app-server generate-ts --out DIR`. Both come from the installed binary, so they are always accurate for the version actually in use rather than for whatever a doc site describes. Use them before recording any Codex protocol fact as unknown — they answer shape questions outright. They do NOT answer behaviour questions: there is not one doc comment on any dynamic-tool type, and no timeout anywhere in `DynamicToolCallParams` or `TurnStartParams`. Behaviour still costs a probe.
- Node 24 is not on `PATH`; it lives in fnm. Prefix commands with `export PATH="$HOME/.local/share/fnm/node-versions/v24.19.0/installation/bin:$PATH"`.
- Read exit codes from `$?` directly. A background wrapper's exit code is not the command's, and a pipeline hides the failure — this cost a false "green" once already this cycle.
- Live suites spend the maintainer's real vendor quota. Ask before running them in bulk.
- `agy` can be driven as a coding agent over this repo headlessly with `--mode accept-edits`; `--dangerously-skip-permissions` is never needed for file edits. Two traps. A *shell* or out-of-workspace *read* request is auto-denied in headless and the run then prints only `jetski: no output produced ...` — but **edits already made do land**, so read `git diff --stat` rather than assuming the run did nothing. And keep every path in the prompt relative to `--add-dir`; repo-root paths are what triggered the denied read. In the model catalog the generation ranks above the tier, so `gemini-3.7-flash-high` is the strongest choice and `gemini-3.1-pro-high` is not.
- The live suites drive the adapter directly against a fake context; nothing in them exercises DSH's agent loop, delegation or presets. Anything about subagents has to be run in the real app. The recipe: `node packages/suite/lib/bin.js preset update` to push the packaged preset into `$DSH_HOME/.agent-presets`, add a `subagent-model-selection` section to `~/.dsh/settings.yaml` (`enabled: true` plus exact `allowedModels` routes), then `dsh web --no-open --port <port>` and drive the UI. Read the result from `~/.dsh/sessions/<workspace>/<session>/session.jsonl.zstd` (`zstd -dc`), not from the model's answer: the child session records `origin: subagent` and its own `request/header` route.
- Selecting a model in the web composer rewrites `agent-default-model` in `~/.dsh/settings.yaml`, and the selection lands on the request after the one already in flight. Switching model and sending in the same moment produced a mid-turn route change — which is how the open Codex defect in *What remains* was found, but it is also a trap when you meant to test something else.
- Core's `tsdown` build is not reproducible: two builds of an unchanged tree differ in CSS-module key order, so byte-level artifact comparison proves nothing.
- Reading source is not enough for vendor behaviour. Three conclusions drawn from code alone were wrong this cycle and only a live run corrected them.

## Hard constraints

- GitHub Actions/hosted CI are not used. Do not inspect or edit `.github/workflows/*`.
- No publish / merge / tag / release without explicit maintainer approval.
- Do not copy, parse, migrate or delete vendor credential/session/token stores.
- Do not reintroduce destructive legacy credential deletion, or any Core credential-mutation surface, without a reviewed atomic-safe credential contract.
- Read command exit codes directly; avoid pipelines that mask failures.
- Windows remains **NOT TESTED**.
