name: runner-smoke

on:
  push:
    branches:
      - feat/market-migration
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo runner-ok
