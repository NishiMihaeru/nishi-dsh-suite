# Third-Party Notices

This package drives the installed official Codex CLI (`codex`) as an external process and uses the DeepSeek Harness subprocess/timeout APIs. No OpenAI runtime package is bundled or redistributed.

Authentication remains owned by the normal Codex client/runtime. Nishi DSH Suite does not copy or broker Codex credentials.

See the repository root `THIRD_PARTY_NOTICES.md` for repository-wide notices.
