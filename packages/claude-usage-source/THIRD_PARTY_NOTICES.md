# Third-Party Notices

This package drives the officially installed Claude Code CLI (`claude`) as an external process and uses the DeepSeek Harness subprocess/timeout APIs. No Anthropic runtime, SDK, or model client is bundled or redistributed.

Authentication remains owned by the normal Claude Code client/runtime. Nishi DSH Suite does not copy or broker Claude credentials.

See the repository root `THIRD_PARTY_NOTICES.md` for repository-wide notices.
