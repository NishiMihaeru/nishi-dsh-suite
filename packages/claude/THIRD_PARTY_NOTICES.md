# Third-Party Notices

This package spawns the user's installed official Claude CLI to read usage and limits. It does not bundle, redistribute, or vendor the Claude CLI, any `@anthropic-ai/*` package, any model runtime, or any credential store.

Claude authentication and session state remain owned by the official Claude Code product boundary; nothing here is copied, parsed, migrated, or deleted.

See the repository root `THIRD_PARTY_NOTICES.md` for repository-wide notices.
