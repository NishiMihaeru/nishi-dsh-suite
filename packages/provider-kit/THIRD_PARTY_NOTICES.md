# Third-Party Notices

This package is a shared runtime library. It resolves, spawns, decodes, and disposes of external vendor CLI processes on the caller's behalf; it does not bundle, redistribute, or vendor any third-party CLI, SDK, model runtime, or credential store.

See the repository root `THIRD_PARTY_NOTICES.md` for repository-wide notices.
